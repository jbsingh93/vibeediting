# `color/` — grading

The biggest objective "AAA" gap. Primary path is deterministic FFmpeg `lut3d`; Python is the fallback.
**Status: BUILT (P1B, 2026-05-27)** except the on-demand ML grade — tested in `_tests/p1b-color.test.ts`.

| File | Purpose | Backs |
|---|---|---|
| `luts/` ✅ | House LUTs `neutral-correct/warm-cine/teal-orange/film-kodak2383.cube` — **GENERATED by `_generate-luts.py`** (license-free, reproducible). | P1B.1 |
| `grade.ts` ✅ | Full-ffmpeg `lut3d=...:interp=tetrahedral` + `--intensity` blend; `format=yuv420p`. **Windows colon footgun sidestepped via cwd=LUT-dir + bare filename (GAP-21).** | P1B.2 |
| `grade.py` ✅ | `colour-science` `read_LUT().apply()` for stills/thumbnails + intensity; slow per-frame video mode (audio re-mux). | P1B.3 |
| `correct.ts` ✅ | Exposure/WB/contrast neutralize (`colortemperature`/`eq`/`colorbalance`) — separate from the look LUT. | P1B.4 |
| `ml-grade/` (Tier-AAA, on-demand) | ICCV `VideoColorGrading`: reference `.jpg` → generated `.cube` → apply via `grade.ts`. | P1B.5; models.json `color.mlGrade` |

**I/O contract:** input video/still + LUT (or reference) → graded output in
`out/work/<project>/color/`; before/after stills + a Gemini diff logged. *(AN §3.4; ICCV arXiv:2508.00548.)*
