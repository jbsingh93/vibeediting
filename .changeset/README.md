# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).
Run `npm run changeset` to record a change; the release workflow opens a
"chore: release" PR and publishes to npm (OIDC trusted publishing) when it merges.
